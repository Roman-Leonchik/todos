import {
    StyledFooter,
    StyledText
} from "./footer.styled"

export const Footer = () => {
    return(
        <StyledFooter>
            <StyledText>Drag and drop to reader list</StyledText>
        </StyledFooter>
    )
}